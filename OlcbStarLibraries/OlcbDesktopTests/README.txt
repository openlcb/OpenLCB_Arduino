To add a new test file:
1) Create the file (by convention only, name ends in _unittest.cpp)
    *) Needs to #include "gtest/gtest.h" and contain TEST macros
2) Add it to the "Source Files" section of the project on the left side of NB screen

That's it, everything else is automatic.
